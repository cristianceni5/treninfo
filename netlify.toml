[build]
  command = "echo No build"
  publish = "."
  functions = "netlify/functions"

# /api/* → /.netlify/functions/api/api/* (nota il doppio /api)
[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/api/api/:splat"
  status = 200
